class MergeSort{
    constructor(collection){
        this.collection = collection;
        this.i = 0;
        this.collection.bars = this.collection.bars.map(bar => [bar]);
    }

    sort(){
        //TODO: add the sort 
    }
}